============
Installation
============

At the command line::

    $ pip install manic_entropy_squall

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv manic_entropy_squall
    $ pip install manic_entropy_squall
